# Copy to .env and set real values. Never commit .env (it is in .gitignore).
# For production: use HTTPS APP_URL, production DATABASE_URL, and set RESEND_* for intake emails.

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/holger_coaching

# Session Secret (generate a random string)
SESSION_SECRET=your-secret-key-here-change-this-in-production

# Server Port (optional, defaults to 3000; avoid 5000/5001 on Mac – used by AirPlay)
PORT=3000

# App URL (for OAuth callbacks and email links)
APP_URL=http://localhost:3000

# Public site URL (for logo and links in client-facing emails; defaults to production)
# PUBLIC_SITE_URL=https://nathanielbaldock.com
# Optional: white/light logo URL for dark header in emails (if logo.png is dark)
# EMAIL_LOGO_URL=https://nathanielbaldock.com/logo-white.png

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Google OAuth (for Sign in with Google - optional)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# PAYMENTS (Stripe + PayPal)
# =============================================================================

# Stripe (https://dashboard.stripe.com/apikeys)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# PayPal (https://developer.paypal.com/dashboard/applications)
# PAYPAL_CLIENT_ID=...
# PAYPAL_CLIENT_SECRET=...
# PAYPAL_MODE=sandbox  # or 'live' for production

# =============================================================================
# GOOGLE CALENDAR SYNC
# =============================================================================

# Google Calendar OAuth (separate from Sign-in OAuth)
# Use the same Google Cloud project, but enable Calendar API
# GOOGLE_CALENDAR_CLIENT_ID=your-calendar-client-id
# GOOGLE_CALENDAR_CLIENT_SECRET=your-calendar-client-secret

# =============================================================================
# FILE STORAGE
# =============================================================================

# Google Cloud Storage (for file uploads - optional for local dev)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
# OR
# GOOGLE_CLOUD_KEY=base64-encoded-service-account-json

# =============================================================================
# EMAIL (Resend - required for intake form notifications and other transactional email)
# =============================================================================
# Intake form submissions send a confirmation to the submitter and a notification
# to SITE_CONTACT_EMAIL (see shared/constants.ts). Without RESEND_API_KEY, no emails are sent.
# In Resend dashboard: add and verify your sending domain (e.g. nathanielbaldock.com).
#
# RESEND_API_KEY=re_xxxxxxxxxxxxx
# RESEND_FROM_EMAIL=Nathaniel Baldock AI Consulting <noreply@nathanielbaldock.com>

# =============================================================================
# ANALYTICS (basic site traffic - set one or both; only active in production build)
# =============================================================================
# Rebuild and redeploy after setting. Client loads the script only when not in dev.
#
# Plausible: sign up at plausible.io, add your domain, then set:
# VITE_PLAUSIBLE_DOMAIN=nathanielbaldock.com
#
# Google Analytics 4: create a GA4 property at analytics.google.com, then set:
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
